# ──────────────────────────────────────────────
#  Hindsight-Powered AI Group Project Manager
#  Python Dependencies
# ──────────────────────────────────────────────

# Streamlit — UI framework
streamlit>=1.35.0

# Groq — LLM provider (qwen/qwen3-32b)
groq>=0.9.0

# Hindsight — cloud memory client (connects to Hindsight Cloud API)
# https://github.com/vectorize-io/hindsight
hindsight-client>=0.4.0

# Environment variables
python-dotenv>=1.0.0

# Async patch — allows asyncio.run() inside Streamlit's already-running loop
nest_asyncio>=1.6.0
